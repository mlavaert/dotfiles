set nocompatible

" Display relative line-numbers
set number
set relativenumber

" Enable backspacing over everything in insert mode
set backspace=indent,eol,start

" Enable syntax highlighting and indening
syntax on
set autoindent
filetype plugin indent on
